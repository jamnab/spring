#new-post.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h4.modal-title
          Create Post
      .slide-1
        = form_for Post.new, :html => { multipart: true },remote: true do |f|
          .modal-body
            = f.hidden_field :user_id, value: current_user.try(:id)
            = f.hidden_field :organization_id, value: current_user.try(:organization).try(:id)
            .field.col-xs-12
              = f.label :title
              = f.text_field :title, class: "form-control"
            .field.col-xs-12.nested_pics_form
              = f.label :images
              = file_field_tag "images[]",type: :file, multiple: true
            .field.col-xs-12
              = f.label 'Departments (select at least 1)'
              = render 'layouts/department_selector', context: current_organization, department_entries: current_organization.department_entries
            .field.col-xs-12
              = f.label :content
              = f.text_area :content, class: "form-control", placeholder: "Enter description...", rows: 4, maxlength: 250
            .field.col-xs-12
              %i Posts are by default anonymous, and the poster identity is always revealed when a post becomes a launch action item.
              - if can? :update, current_organization
                = f.label 'Post as Anonymous'
                = f.check_box :anonymous
          .modal-footer
            .actions
              = f.submit 'Create Post', class: "btn btn-main"
